[project]
name = "coracle"
version = "0.1.0"
description = "Coracle Regional Burn website"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1",
    "django-tasks-db>=0.3.0",
    "qrcode[pil]>=8.0",
    "stripe>=14.3.0",
]
